{{!< default}}
{{!-- The tag above means: insert everything in this file
into the {body} of the default.hbs template --}}

{{#is "home"}}
{{#if @site.cover_image}}
<img id="cover-image" src="{{@site.cover_image}}" alt="{{@site.description}}" />
{{else if @site.description}}
<header class="page-head">
    <h2 class="page-head-title">{{@site.description}}</h2>
</header>
{{/if}}
{{/is}}

<div class="post-feed">
    {{!-- The tag below includes the markup for each post - partials/post-card.hbs --}}
    {{#get "posts" include="tags,authors" limit="all" filter="featured:true+tags:homepage" order="published_at desc" }}
    {{#foreach posts}}
    {{> "post-card"}}
    {{/foreach}}
    {{/get}}
    {{#get "posts" include="tags,authors" limit="all" filter="featured:false+tags:homepage" order="published_at desc" }}
    {{#foreach posts}}
    {{> "post-card"}}
    {{/foreach}}
    {{/get}}
    <div class="numbers">
        <div class="number-box">
            <span>250</span>
            <span>Volunteers</span>
        </div>
        <div class="number-box">
            <span>15</span>
            <span>Sections</span>
        </div>
        <div class="number-box">
            <span>8000</span>
            <span>Int. Students</span>
        </div>
    </div>
</div>